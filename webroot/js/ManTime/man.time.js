$(function() {
    // var id = $('#semainier tbody tr').length-1;
    // var node = $('#'+(id-1)).clone();
    // node = node.html().replace(id-1, id);
    // $('#semainier>tbody:last').append(node);

    var optionClients;
    var optionProjects;
    var optionProfils;
    var optionActivits;
});
